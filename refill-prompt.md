# 任务：给野生动物选题补「为什么值得读」（单批增量）

你是一个只做一件事的机器人：给**末尾「本批任务」里列出的物种**，每个补两样东西 ——
一句话钩子（`title`）和值得写的角度（`note`）。

**不要探索目录，不要读取其他文件，不要执行命令。** 你需要的全部输入都在末尾。
唯一允许的工具是 **Write**，唯一允许写的文件是 `data/queue.add.tsv`。

> **绝对不要写 `data/queue.tsv`。** 那是选题池本体，由脚本验收后追加。
> 你写的是只含新行的增量文件，脚本会逐行验收，不合格的行会被丢弃。

---

## 你**不需要**提供物种名

物种名（`subject`）已经定好了，而且已经过了四道闸门：学名格式、分类阶元、
排除家养种、排除统称、以及「中文维基上确有这个条目且正文里出现该物种学名」。

所以：

- **照抄末尾给你的 `subject`，一个字都不要改。** 不要转繁简，不要加「的」，
  不要换成你觉得更常见的叫法。改了就认领不到，整行作废。
- **不要自己新增物种。** 末尾没列的物种，写了也会被丢弃。
- 学名、类群、地理界、维基标题这些列**不用你写**，脚本会自己查出来填。

你的活儿只有一样：**这一条为什么值得读。** 那才是模型比脚本强的地方。

---

## 输出格式

`data/queue.add.tsv` 是**制表符分隔**的纯文本，**3 列**，无表头，只含新增行：

```
subject<TAB>title<TAB>note
```

| 列 | 要求 |
|---|---|
| `subject` | 照抄末尾给出的物种名，一字不差 |
| `title` | 一句话钩子，**6–26 字** |
| `note` | 这一篇打算讲的那个具体的点，**8–48 字** |

---

## `title` 写成什么样

钩子要落在**一个具体的、可以被查证的事实**上，不是形容词。

好的：

```
用尿液标记两百平方公里的独居者
一生只喝一次水的沙漠羚羊
把猎物拖上树以躲开狮子
能听见三十公里外同类叫声的低频信号
```

坏的（这些会被脚本直接判废）：

```
薮猫的介绍            ← 空泛模板
狷羚是一种非洲羚羊      ← 是词典释义，不是钩子
神秘的高角羚          ← 「神秘」是形容词，没有信息
猎豹                 ← 就是物种名本身
```

被判废的词面：`的介绍` `的简介` `简介` `是一种` `的故事` `小知识` `科普`
`带你了解` `你知道吗` `揭秘`。

---

## `note` 写成什么样

一句话交代**这一篇的角度**，写给后面负责写正文的人看。要具体到能据此动笔。

```
薮猫    跳跃捕猎的成功率与耳朵定位精度的关系，命中率约五成
旋角羚  野外功能性灭绝但圈养种群上千，讲「保住了物种没保住野性」
大熊猫  食肉动物的消化道吃竹子，靠肠道菌群和极低同化率硬扛
```

---

## 三条硬约束（这个项目的立项前提）

1. 写的必须是**一种具体的野生动物**，不是一类动物的宽泛介绍。
   `subject` 已经保证了这一点，所以 `title` 和 `note` 也别往「猫科动物都……」
   那个方向写。
2. 不要提**家养动物**（家猫、家犬、家牛）作为主角。
3. 角度可以冷门，但必须是**真的**。你不确定的数字就不要写进 `note` ——
   后面有一道 0 token 的事实校验，编的数字会被拦下来，整条选题白费。

---

## 本批要写多少行

末尾列出几个物种就写几行，不多不少。少写会触发补池告警，多写的部分会被丢弃。

---

## 本批任务
