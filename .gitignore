# 216MB 的 zhwiki 索引明文。与 wiki-bot 共用一份，靠 .env 里的 ZH_INDEX 指过去，
# 不在这个仓库里存也不重复下载。
.cache/

# 导入日志。每次重跑都会变，且几千行翻页记录进版本历史没有价值。
data/import.log

# 建池日志。同上 —— 结论（各类群条数、拒因分布）已经写进 SPEC，过程无价值。
data/build-queue.log

# refill 的中间文件：拼给 agent 的 prompt、agent 写的增量、验收后的合格行。
# 每一批都被覆盖，且 run.sh 跑完就删 —— 入库只会留下半截的残骸。
data/.refill-prompt.txt*
data/queue.add.tsv*

# 运行日志（run.sh 按日期落盘）。
logs/

# 取题结果（pick.py 每天覆盖）。它是当天的中间态，不是配置 —— 入库只会制造
# 每日一次的无意义 diff。与 wiki-bot 同处理。
pick.json

# agent 当天写的正文（每天覆盖）。**归档件 data/content/*.json 要入库**（阶段 8 靠它
# 做 0 token 重渲），根目录这一份只是本次运行的暂存。同 wiki-bot。
content.json

# 每日阶段/通知/构建标记。
state/

# 出图请求流水（date/kind/url/prompt）。SPEC §11 早就写了它该被忽略，实现时漏了 ——
# 阶段 9 装 publish.sh 的时候才发现它以未跟踪文件的形式长期挂在 git status 里。
# 不入库的理由：URL 是带签名的临时地址，几小时后就失效，入库只留一堆过期串。
# **但本机这份要留着**：物种画错是本项目最主要的失败模式，事后追查唯一有用的证据
# 就是「当时到底把什么 prompt 发出去了」，而那只在这个文件里。
img_urls.jsonl

# openclaw agent 在 workspace 里自动生成的引导文件与运行状态。
# 都是默认模板（IDENTITY.md 里还写着"Fill this in during your first conversation"），
# 不是本项目的配置 —— wiki-bot 的 .gitignore 也是这么处理的。
AGENTS.md
BOOTSTRAP.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
openclaw-workspace-state.json
.openclaw/

# refill 的原始 stdout（含 openclaw 的插件告警等噪音）。
data/refill.out

# 进度文件，只对本机的那一次导入有意义。
data/.import-progress.json

# 调研脚本的输出。结论已经写进 SPEC.md 和代码注释，原始输出不必入库。
probe/*.txt

__pycache__/
*.pyc
.env
